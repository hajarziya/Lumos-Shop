import { makeStyles } from '@src/theme'

export default makeStyles()((theme) => ({
  card: {
    width: '300px',
    height: '400px',
    backgroundColor: 'transparent'
  },
  button: {
    marginTop: '30px',
    fontWeight: 'bold'
  }
}))
