import { makeStyles } from '@src/theme'

export default makeStyles()((theme) => ({
  container: {
    display: 'flex',
    justifyContent: 'space-between',
    margin: '30px',
    gap: '40px'
  },
  imgWrapper: {
    width: '500rem',
    height: '500px'
  }
}))
